# Crash suggestion — main

**When:** 2026-08-10 21:29:14

**Exception:** `SyntaxError: ':' expected after dictionary key (tool_declarations.py, line 263)`

## Gemini's suggestion (unreviewed — do not apply blindly)

**Root Cause:**
A `SyntaxError` occurred in `core/tool_declarations.py` at line 263 because the `description` string contains an unescaped Python dictionary-like literal (`steps=[{tool,args}...]`). Python’s parser interprets this as a malformed inline key-value definition inside a JSON/dictionary context, missing a colon after the dictionary key.

**Suggested Fix:**
Escape the curly braces or remove the pythonic dict notation from the natural language description string. 

Change line 263 to:
```python
"description": "User-defined routines/macros. Actions: list, save (name, steps=[{'tool':..., 'args':...}], description), run (name), delete (name), get (name)..."
```
Ensure all `{` and `}` characters inside string literals are properly escaped or replaced with plain text equivalents so the Python parser doesn't mistake them for code syntax.

## Traceback
```
Traceback (most recent call last):
  File "C:\Users\notde\Desktop\Gama 2.0\main.py", line 273, in <module>
    from core.tool_declarations import PROCESSING_ACK_LINES, TOOL_DECLARATIONS, get_filtered_declarations
  File "C:\Users\notde\Desktop\Gama 2.0\core\tool_declarations.py", line 263
    {"name": "routines", "behavior": "NON_BLOCKING", "description": "User-defined routines/macros. Actions: list, save (name, steps=[{tool,args}...], description), run (name), delete (name), get (name). Example: save name='study mode' with steps to open VS Code, mute, play music.",
                                                                                                                                                                                                                                                                    ^^^^^^^^^^^^^^^^^^^^^^
SyntaxError: ':' expected after dictionary key

```
